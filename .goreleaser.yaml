version: 2

builds:
  - id: gateway
    main: ./cmd/gateway/
    binary: gateway
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - id: gateway
    ids: [gateway]
    name_template: "vanta_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

dockers_v2:
  - id: vantaproxy
    ids: [gateway]
    dockerfile: Dockerfile.goreleaser
    images:
      - "ghcr.io/darks0l/vantaproxy"
    tags:
      - "v{{ .Version }}"
      - latest
    platforms:
      - linux/amd64
      - linux/arm64

release:
  header: |
    ## VantaProxy — What's New

    See the full changelog below for details.

    ### Docker

    ```
    docker pull ghcr.io/darks0l/vantaproxy:v{{ .Version }}
    ```

changelog:
  use: github
  abbrev: 7
  format: "[{{ .SHA }}](https://github.com/darks0l/VantaProxy/commit/{{ .SHA }}): {{ .Message }}"
  sort: asc
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug Fixes
      regexp: '^.*?(fix|bug)(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Security
      regexp: '^.*?[Ss]ecurity.*$'
      order: 2
    - title: Other Changes
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "Merge pull request"
